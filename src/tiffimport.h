/*
#pragma once

#include <string>
#include <vector>

namespace tiffimg {
    struct GeoTiffImage {
        std::vector<short> data;
    };

    GeoTiffImage importGeoTiffFromFile(const std::string && path);
}
*/